import { type AppType } from "next/dist/shared/lib/utils";
import "~/styles/globals.css";
import '../styles/burger.css'

const About: AppType = ({ Component, pageProps }) => {
  return <Component {...pageProps} />;
};

export default About;
