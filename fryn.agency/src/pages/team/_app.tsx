import { type AppType } from "next/dist/shared/lib/utils";
import "~/styles/globals.css";
import '../styles/burger.css'

const Team: AppType = ({ Component, pageProps }) => {
  return <Component {...pageProps} />;
};

export default Team;
